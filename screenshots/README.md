# UI verification screenshots

Screenshots are captured from the running local application, not design mockups. dashboard-desktop.png and dashboard-mobile.png document the default demo workspace at desktop/mobile widths. Historical assessment/scan counts can vary as the demonstration is repeated. The synthetic labels remain visible.
